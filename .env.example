# If you deploying via docker compose you don't need to update these values:
# - POSTGRES_HOST
# - POSTGRES_PORT
# - REDIS_HOST
# - REDIS_PORT

# should be 'off' in prod
DEBUG=on

# options:
# - dev (for local development)
# - prod (for deployed versions)
ENVIRONMENT=dev

SECRET_KEY="super-secret-key"

SITE_URL=http://localhost:8000

# Set to False to pause new account creation while keeping existing logins working.
ALLOW_SIGNUPS=True

POSTGRES_HOST=db
POSTGRES_DB=rowset
POSTGRES_USER=rowset
POSTGRES_PORT=5432
POSTGRES_PASSWORD=rowset

REDIS_HOST=redis
REDIS_PASSWORD=rowset
REDIS_PORT=6379

# If you want to enable Social Auth via Github
# Get his values after creating a new app on GitHub: https://github.com/settings/applications/new
# For Homepage URL use: http://localhost:8000
# For Authorization callback URL use: http://localhost:8000/accounts/github/login/callback/
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Google OAuth enables basic Google signup/login.
# Create an OAuth client in Google Cloud and use:
# http://localhost:8000/accounts/google/login/callback/
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Optional private image asset storage for dataset image columns.
# Leave ROWSET_ASSET_S3_ENDPOINT_URL blank to store private assets on disk.
ROWSET_ASSET_S3_ENDPOINT_URL=
ROWSET_ASSET_STORAGE_BUCKET_NAME=
ROWSET_ASSET_ACCESS_KEY_ID=
ROWSET_ASSET_SECRET_ACCESS_KEY=
ROWSET_ASSET_REGION_NAME=auto

# Cloudflare R2 example:
# ROWSET_ASSET_S3_ENDPOINT_URL=https://<account-id>.r2.cloudflarestorage.com
# ROWSET_ASSET_STORAGE_BUCKET_NAME=rowset-assets
# ROWSET_ASSET_ACCESS_KEY_ID=<r2-access-key-id>
# ROWSET_ASSET_SECRET_ACCESS_KEY=<r2-secret-access-key>
# ROWSET_ASSET_REGION_NAME=auto
#
# Local MinIO private asset example:
# ROWSET_ASSET_S3_ENDPOINT_URL=http://localhost:9000
# ROWSET_ASSET_STORAGE_BUCKET_NAME=rowset-assets-dev
# ROWSET_ASSET_ACCESS_KEY_ID=rowset-user
# ROWSET_ASSET_SECRET_ACCESS_KEY=rowset-password

# If you need email sending capabilities
# This is not really necessary in the state of the app
# Apart from confirming the email.
# If you don't specify the key you will get the link to confirm email
# in your logs
MAILGUN_API_KEY=

# If you plan on doing a newsletter for subscribers
# Not really needed fo custom deployments, I think
BUTTONDOWN_API_KEY=

# Stripe Integration, not needed unless you want to sell access
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_CONTEXT=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID_ROWSET_PRO_MONTHLY=

# Optional semantic row search backed by Qdrant.
ROWSET_VECTOR_SEARCH_ENABLED=False
QDRANT_URL=
QDRANT_API_KEY=
QDRANT_COLLECTION_PREFIX=rowset
QDRANT_TIMEOUT_SECONDS=10
ROWSET_EMBEDDING_MODEL=openai/text-embedding-3-small
ROWSET_EMBEDDING_DIMENSIONS=1536
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_API_KEY=

# MJML URL for self-hosted server
MJML_URL=http://mjml:15500

# Depending on what AI provider you use outside vector search, set the matching API key.
# Vector search, indexing, and backfill use OPENROUTER_API_KEY above.
OPENAI_API_KEY=

LOGFIRE_TOKEN=
LOGFIRE_CONSOLE_SHOW_PROJECT_LINK=False

SENTRY_DSN=
SENTRY_RELEASE=
SENTRY_TRACES_SAMPLE_RATE=1.0
SENTRY_PROFILE_SESSION_SAMPLE_RATE=1.0
SENTRY_ENABLE_LOGS=True
SENTRY_ENABLE_METRICS=True
SENTRY_SEND_DEFAULT_PII=False
SENTRY_INCLUDE_LOCAL_VARIABLES=False
SENTRY_MAX_BREADCRUMBS=100
POSTHOG_API_KEY=

# Chatwoot website widget. Leave either value empty to disable support chat.
CHATWOOT_BASE_URL=
CHATWOOT_WEBSITE_TOKEN=
# Optional: set if identity validation is enabled for the Chatwoot website inbox.
CHATWOOT_HMAC_SECRET=

# Optional Apprise notification URLs for Rowset feedback.
# Add a Slack Apprise URL here to forward browser, REST, and MCP feedback to Slack.
ROWSET_FEEDBACK_APPRISE_URLS=
ROWSET_FEEDBACK_APPRISE_TITLE="New Rowset feedback"
